{
  "name": "ata-coder-web",
  "private": true,
  "description": "ATA Coder Web UI — browser-based frontend served by the HTTP API server (server.py)",
  "engines": {
    "node": ">=24"
  },
  "scripts": {
    "build": "tsc",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "typescript": "^5.7.0"
  }
}
